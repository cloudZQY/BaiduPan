import * as React from 'react';

import CounterContainer from '../view/Count';

const Application = () => (
    <div>
        Hello World from Electron!
        <CounterContainer />
    </div>
);

export default Application;
